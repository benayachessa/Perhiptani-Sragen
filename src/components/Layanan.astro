---
// src/components/Layanan.astro

const layanan = [
  { judul: "Koperasi Tani", deskripsi: "Penyediaan saprotan berkualitas.", icon: "🏦" },
  { judul: "Klinik Konsultasi", deskripsi: "Layanan diagnosa hama penyakit.", icon: "👨‍⚕️" },
  { judul: "Pemasaran Digital", deskripsi: "Membantu petani go-digital.", icon: "📈" },
  { judul: "Diklat & Sertifikasi", deskripsi: "Pelatihan peningkatan kapasitas.", icon: "🎓" }
];
---

<section id="layanan" class="py-24 bg-slate-50 text-slate-800 relative">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
    <div class="flex flex-col md:flex-row justify-between items-end mb-12 pb-6 border-b border-slate-200">
      <div class="max-w-2xl">
        <span class="text-green-600 font-bold tracking-wider uppercase text-sm">Unit Usaha</span>
        <h2 class="text-3xl md:text-4xl font-bold text-blue-950 mt-2">Layanan & Program Kerja</h2>
      </div>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
      {layanan.map((item) => (
        <div class="bg-white p-8 rounded-2xl shadow-sm hover:shadow-xl transition-all duration-300 border-t-4 border-transparent hover:border-green-500 group">
          <div class="w-14 h-14 bg-green-50 rounded-xl flex items-center justify-center text-3xl mb-6 group-hover:bg-green-600 group-hover:text-white transition-colors duration-300">
            {item.icon}
          </div>
          <h3 class="text-xl font-bold text-slate-800 mb-3 group-hover:text-green-600 transition-colors">{item.judul}</h3>
          <p class="text-slate-500 leading-relaxed text-sm">{item.deskripsi}</p>
        </div>
      ))}
    </div>
  </div>
</section>